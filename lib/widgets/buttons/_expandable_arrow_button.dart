part of 'package:reusable_package/widgets/buttons/buttons_widgets.dart';

class _ExpandableArrowButton extends StatelessWidget {
  final bool isExpanded;

  const _ExpandableArrowButton({
    required this.isExpanded,
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
